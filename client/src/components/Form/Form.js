import React from 'react';
import useStyles from './styles';

const Form = () => {
    const classes = useStyles();

    return (
        <div>
            ola senior
        </div>
    )
}

export default Form;
